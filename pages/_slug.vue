<template>
  <!-- <div>
    <article>
      <div v-if="data">
        <h1>{{ data.title }}</h1>
        <p>{{ data.publishedAt }}</p>
        <div v-html="data.body"></div>
      </div>
      <p v-else>コンテンツの読み込みに失敗しました。再読み込みしてください。（command/control + R）</p>
    </article>
  </div> -->
</template>

<script setup>
  // import { useRoute } from 'vue-router'
  // import { useMicroCMSGetListDetail } from 'nuxt-microcms-module'

  // const route = useRoute()
  // const slug = route.params.slug

  // const { data, error } = await useMicroCMSGetListDetail({
  //   endpoint: 'blog',
  //   contentId: slug
  // })

  // if (error) {
  //   console.error(error)
  // }
</script>